import test from 'node:test';import assert from 'node:assert/strict';import {RoomModel,credentials} from '../src/model.js';
test('GOD exit via Normal enables automatic tilt',()=>{let m=new RoomModel();m.command({type:'auto',value:false});m.command({type:'mode',value:'god'});assert.equal(m.snapshot(0).appearance.options.style,'god');m.command({type:'mode',value:'normal'});assert.equal(m.settings.auto,true);assert.equal(m.snapshot(0).appearance.level,0);});
test('sustained shout only; silence closes mouth and stale peer releases microphone',()=>{let m=new RoomModel();m.audio('a',{active:true,level:.9},0);assert.equal(m.snapshot(0).performance.shouting,false);m.audio('a',{active:true,level:.9},300);assert.equal(m.snapshot(300).performance.shouting,false);m.audio('a',{active:true,level:.9},600);assert.equal(m.snapshot(600).performance.shouting,true);m.audio('a',{active:true,level:0},700);assert.equal(m.snapshot(700).micActive,false);assert.equal(m.snapshot(1700).micOwner,null);assert.equal(m.snapshot(1700).performance.shouting,false);});
test('another microphone cannot override live owner; accepts after expiry',()=>{let m=new RoomModel();assert.ok(m.audio('a',{active:true,level:.4},0));assert.equal(m.audio('b',{active:true,level:.9},100),false);assert.equal(m.audio('b',{active:true,level:.3},1100),true);});
test('smoking survives voice; GOD survives shouting',()=>{let m=new RoomModel();m.command({type:'smoking',value:true});m.command({type:'mode',value:'god'});m.audio('a',{active:true,level:1},0);m.snapshot(0);m.audio('a',{active:true,level:1},600);let s=m.snapshot(600);assert.equal(s.lounge,'smoking');assert.equal(s.appearance.options.style,'god');assert.equal(s.performance.shouting,false);});
test('reject invalid commands and audio values',()=>{let m=new RoomModel();assert.equal(m.command({type:'setting',key:'__proto__',value:1}),false);assert.equal(m.audio('x',{active:true,level:NaN},0),false);assert.equal(m.audio('x',{active:true,level:2},0),false);assert.equal(m.command({type:'mode',value:'evil'}),false);assert.equal(credentials('#room=x&key=y'),null);assert.ok(credentials('#room='+'a'.repeat(32)+'&key='+'b'.repeat(64)));});
